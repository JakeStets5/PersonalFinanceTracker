<UserControl x:Class="PersonalFinanceTracker.Views.UploadTransactionView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:PersonalFinanceTracker.Views"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="2*"/>
            <!-- Income & Expense section -->
            <ColumnDefinition Width="1*"/>
            <!-- Breakdown section -->
        </Grid.ColumnDefinitions>

        <StackPanel Grid.Column="0" Orientation="Vertical" Margin="10">
            <!-- Income Section -->
            <GroupBox Header="Income" Margin="5">
                <StackPanel>
                    <TextBlock Text="Income Details" FontSize="14"/>
                    <!-- Income fields, bindings, and buttons here -->
                    <TextBox Width="200" Text="Enter Income..."/>
                    <Button Content="Add Income" Width="100" Margin="5"/>
                </StackPanel>
            </GroupBox>

            <!-- Expense Section -->
            <GroupBox Header="Expenses" Margin="5">
                <StackPanel>
                    <TextBlock Text="Expense Details" FontSize="14"/>
                    <!-- Expense fields, bindings, and buttons here -->
                    <TextBox Width="200" Text="Enter Expense..."/>
                    <Button Content="Add Expense" Width="100" Margin="5"/>
                </StackPanel>
            </GroupBox>
        </StackPanel>

        <!-- Breakdown Section -->
        <GroupBox Header="Breakdown" Grid.Column="1" Margin="10">
            <StackPanel>
                <TextBlock Text="Spending Breakdown" FontSize="14"/>
                <!-- Example: Pie Chart or Bar Chart placeholder -->
                <Border Background="LightGray" Height="200" Width="200" Margin="5">
                    <TextBlock Text="[Chart Placeholder]" VerticalAlignment="Center" HorizontalAlignment="Center"/>
                </Border>
            </StackPanel>
        </GroupBox>

    </Grid>
</UserControl>
